# Render Backend Environment Variables
# Set these in Render Dashboard under Environment

# Server Configuration
PORT=8080
NODE_ENV=production

# Database Configuration
# Use Render PostgreSQL or any managed PostgreSQL service
DATABASE_URL=postgresql://username:password@your-db-host:5432/database_name

# JWT Secrets - Generate secure secrets
JWT_ACCESS_SECRET=your_access_secret_change_this
JWT_REFRESH_SECRET=your_refresh_secret_change_this

# JWT Expiration Times (in seconds)
JWT_ACCESS_EXPIRES=900
JWT_REFRESH_EXPIRES=1209600

# CORS Configuration - Update with your Vercel frontend domain
CORS_ORIGINS=https://your-domain.vercel.app
